# protoc-gen-es emits a blank line at EOF; generated protocol sources are the
# only files exempt from the blank-at-eof diff check.
sdk/protocol/src/gen/** whitespace=-blank-at-eof

# Store the Windows wrapper as normalized text and check it out with CRLF.
apps/mobile-shell/android/gradlew.bat text eol=crlf
