# GLM-5.3 接手：完成 WorkOS 剩余功能并反复检查

你是本任务的接手实现者。请实际修改、运行、检查和修复代码，不要只给计划。
用户要求：修复功能与 bug、改善界面、保持代码简洁，不需要保留旧实现兼容层。
上一检查点已按用户要求合并到本地 main；从现在起在下述唯一任务分支完成剩余工作。
**此前合并只是检查点交接。你的任务是完成原计划六条工作流的全部剩余软件实现、bug 修复、
界面收口和最终验收，不是只做 R2/R3/R5 的几个示例。不允许自行缩减范围或把总任务提前改成 done。**

用户后续补充：必须在唯一任务分支开发。以下单分支规则优先于旧交接中“从 main 新建分支”
的表述；此前合并授权已用于检查点，不是允许你今后不断合并 main 的长期授权。

## 1. 先读这些事实，再动代码

工作目录：`/home/aquatao/workos`。已合并检查点为 `d4fca63`，执行时读取真实分支和工作树，
不要 reset 到文档里的哈希。此文档的后续修订在唯一任务分支上继续。

### 1.1 单分支与 main 保护：必须实际遵守

**不能假设远端存在分支保护，Git 允许执行不等于用户授权。** 本任务固定：

- 唯一开发分支：`feat/v1-remaining-capability-sweep`，已存在，直接复用。
- 唯一 worktree：`/home/aquatao/workos`；唯一任务记录：
  `docs/tasks/20260903-v1-remaining-capability-sweep.md`；唯一写入实现者：当前接手 Agent。
- R1–R6 的开发、文档、测试修复、审查修复、阶段提交均在这个分支串行完成。
  禁止另建 feature/fix/review/candidate/backup 分支、临时 worktree，禁止派第二个 Agent 改文件。
- `main` 只用于只读比较。禁止在 main 编辑或提交，禁止更新 main 的 ref，禁止 merge、
  squash、reset、rebase、cherry-pick 到 main；不执行 push 或 force push。
- 只有用户**后续明确授权合并本轮剩余工作**，才执行最终合并；测试全绿或应用 promote
  都不是 Git 合并授权。正常完成时停在唯一任务分支的干净 HEAD，提供结果供用户审查。
- “只有一个分支”指本任务只有一个开发分支，加上只读 main；仓库中其他历史分支保持原样，
  不为满足数量表述删除它们。不要 stash/reset 掩盖未知改动或删除重建已有任务分支。

开始时依次执行并记录结果：

```sh
git status --short --branch
git branch -vv
git worktree list
git log -8 --oneline
git merge-base main feat/v1-remaining-capability-sweep
```

确认工作树改动归属后，若当前不在指定分支，切到已有分支：

```sh
git switch feat/v1-remaining-capability-sweep
test "$(git branch --show-current)" = 'feat/v1-remaining-capability-sweep' || exit 1
```

**每次会话恢复、开始写文件、运行可能改文件的 generate/format、提交前，重新执行分支断言。**
断言失败先停止该写操作；核对后切回指定分支继续，不能删除断言或因为“main 没保护”继续写。
切换会覆盖未知改动时保留原状，先弄清归属，不强制切换。目标分支意外缺失时先核对任务历史，
不要自行另起名字；本仓库正常接手不需要创建新分支。

### 1.2 资料与事实核查

依次阅读：

1. 根目录 `AGENTS.md`。
2. `docs/structure.md`：产品架构；`docs/architecture/implementation.md`：实际代码边界。
3. `docs/status.json`：阅读 evidence 全文，不能只看模块总状态 working。
4. `docs/tasks/20260903-v1-remaining-capability-sweep.md`：认领这个总任务，在末尾续写。
5. `docs/prompts/20260903-next-agent-remaining-capability-sweep.md`：原完整需求和失败矩阵。
6. ADR-0016、0020、0022、0023、0024、0025，以及目标模块对应 Proto、现有实现和测试。
7. 涉及 UI 时先读 `docs/ui/README.md`，查看当前截图后再设计。

**旧任务记录包含已被后续审查否定的结论。** 例如 2026-09-05 的“所有门禁 PASS”，
不能证明今天的完整能力。原 mobile wrapper 门禁曾把缺少工程当成 SDK 阻塞；旧远程
surface 门禁只证明静态 fixture；旧 repair 门禁只证明提交任务。以最新修复段、代码和
真实可复现证据裁决。不要恢复这些错误验收方式。

## 2. 已有证据与完整任务范围

以下是已落地的主要边界，先复用，再补缺口。已有证据是基线，不是免检名单；发现可复现 bug 必须修复：

| 范围             | 已有实现与证据                                                                                                                    | 仍然不能据此声称                                   |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 桌面             | 完整画布、Mission Control/Home、统一工具入口、拖动/缩放/吸附/最小化/恢复、响应式导航；21 组前后截图、三种尺寸、真实后台浏览器回归 | 原生 Terminal、Remote Browser、WebRTC 已完成       |
| App Bridge       | own-window、project/theme、files、artifacts create/open 的授权、撤销、版本重验；真实跨进程测试                                    | App 获得任意宿主路径或其他窗口权限                 |
| 知识             | 离线多语言模型、pgvector 384 维、工作区增量/删除/停用/重建、缓存、确定性混合分页、App scoped 查询、archive                        | 可用随机向量/词项哈希替代语义模型                  |
| 通知             | Core durable outbox、RFC 8291/8292 加密、503 后重启重试、Chromium wake/catch-up、设备撤销传播                                     | 真实 APNs/FCM 投递或移动原生工程已完成             |
| Repair admission | owner/project/incident/installation 校验、固定入队版本与项目 revision、精确幂等、健康 Provider 检查、候选能力检查                 | Recovery fallback、完整 L3 审批/预算/断路已接入    |
| Repair sources   | Registry 自有源码包、固定 manifest build 配置、有效 lease 解析与提交、单一不可变候选、completed task 私有读取                     | 已构建镜像、测试通过、已有可部署新版本             |
| Harness          | CLI v2 canonical 输入输出、最小环境、进程组清理、流预算、review/context/runtime deadline、真实候选产出                            | Generic CLI 有 token/cost budget、usage 或内核隔离 |
| Deployment       | durable 候选台账、精确版本切换/启动、启动阶段及 canary 故障观察、回滚重试、公平轮询                                               | Build/Test → Registry version → Offer 已连接       |

桌面主要视觉证据：
`docs/ui/desktop-web/changes/20260907-desktop-canvas/{before,after,notes.md}`，
以及 `docs/ui/desktop-web/current/`。尺寸为 1440×900、820×1180、390×844。
不要重新引入占满桌面的永久侧栏。Browser 当前是隔离的网页窗口；Terminal 仍明确不可用。

重要历史提交可用 `git show` 阅读：
`79dd8e2` 桌面画布；`bf20dd0` 加密推送组合链；`bb835e0` 真实模型/pgvector；
`4722a40` 大批量模型缓存；`7694941` 部署/回滚；`350248e` Repair 目标快照；
`1eb4a32` 健康入队；`301cf41` CLI v2；`30bc8a7` 源码/build 配置；
`91ce9af` lease-bound 候选。其后的本次合并包含真实 Harness 候选 producer 和消费者读取。

### 2.1 你必须交付的整批工作

原计划的 W1–W6 与修复记录的 R1–R6 是相同六类范围，不是两批可任选的需求。
先把原文每条“范围内”、阶段和失败矩阵映射到总任务记录中的验收行。每行必须包含：
需求来源、当前实现入口、已知缺口、下一步动作、测试入口、PASS/FAIL/BLOCKED、证据路径。
有对应实现就验证并修复缺陷，缺实现就补齐，不能把“已审查”当作验收。

| 工作流            | 本轮必须交付                                                                            | 可结束该项的证据                                      |
| ----------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| R1 Provider/凭据  | 所有已接入 Provider 的绑定、真实 fixture 执行、凭据租约与轮换、能力声明及失效链核查修复 | Core/Harness/浏览器组合链，失败/重启/撤销/预算矩阵    |
| R2 Runtime/自愈   | Build/Test、候选暂存、发布/回滚、Recovery 治理，以及监督/遥测完整收口                   | 从真实故障到可用修复版本的完整链，失败零误发布        |
| R3 Surface/Bridge | Browser Pool、Native/WebRTC、PTY，与 Bridge/Declarative 授权生命周期完整集成            | 可交互画面/输入、工作区访问、资源与撤权/重启矩阵      |
| R4 工作区/知识    | 全部源管理、真实模型摄取、混合搜索、archive、App scope 的遗漏与 bug 修复                | 用户完整路径和并发/索引重建/归档/分页/模型不可用证据  |
| R5 通知/移动/LAN  | 可启动原生 wrapper、推送全链、设备安全存储、发现/指纹/配对/Transport 收口               | Web 与平台软件工程可复现，通知与配对组合门禁          |
| R6 桌面/交互      | 系统应用全部接通、真实 Browser/Terminal、统一界面质量与多尺寸操作收口                   | 每个入口有真实行为，关键状态 E2E 与逐张审阅的视觉证据 |

建议执行顺序：先快速复核 R1 基线，再 R2 → R3 → R4 → R6 → R5，最后六条链联合验收。
R2 中依赖 R3 的部分先完成契约和可独立验证实现，再接通依赖并回到未完成项。
同一分支逐阶段提交，不以工作量大、一次会话结束、某个小门禁通过为停止条件。
遇到真正环境阻塞，记录精确证据并继续其他不依赖它的工作，最后回来复查。
**不得只交下一份交接文档、只补测试或只做样例；目标是剩余软件能力完整可用。**
不额外加入原计划明确排除的多 owner 协作、付费服务、知识图谱或第七个常驻进程。

## 3. 紧接着做 R2：构建、测试、版本交接

### 3.1 已有链路的准确入口

- `api/proto/workos/app/v1/source.proto`：公开 owner-scoped Source Create/Get。
- `schemas/workos-app-manifest-v1.schema.json`：唯一 manifest Schema，含可选 build 配置。
- `api/proto/workos/taskexecution/v1/repair.proto`：
  - `RepairExecutionService` 仅位于 Core 的 Harness mTLS listener。
  - `ResolveRepairBuildInput` 根据有效 lease 返回固定版本、源码和 build/test argv。
  - `SubmitRepairSourceCandidate` 只接收文件，不接收 owner/版本/权限/测试配置。
  - `RepairCandidateService.GetRepairSourceCandidate` 位于普通 Core 私有 listener，
    根据服务身份的 owner 读取 completed task；Gateway 不开放。
  - 返回原始构建输入、候选源码和 project/incident，用于下一段 Build/Test。
- `internal/core/orchestration/repair_sources.go`：事务、任务来源与失效检查。
- `internal/core/appregistry/application/build.go`：固定配置和候选读取/保存。
- `internal/harness/worker/worker.go`：执行前解析；提交候选并验证回执；禁止缺候选的成功终态。
- `internal/harness/adapters/genericcli/`：完整成功流和 exit 0 后才发布候选。
- `internal/reliability/application/deployment.go`：`HandleRepairCompleted` 仍返回
  `ErrDeploymentCandidateRequired`，这是尚未接通的位置，不是可以删除的保护。

源码上限：128 个文件、单文件 256 KiB、总计 512 KiB；普通文件清单，不接受 ZIP/TAR、
链接或宿主路径。摘要包含路径、字节、执行位。manifest 固定 sourceBundleId/sourceDigest、
不可变 baseImage 和 buildCommand/testCommand。候选只能换文件，不能改构建权限与配置。

`make test-repair-producer` 已证明真实 Gateway/Core/Harness/CLI/PG 链路：CLI fixture 将
示例 main.go 的 return 0 改为 return 42，原测试文件保持一致，候选重启后不变。
**这个门禁没有执行示例的测试或构建镜像。不要把“候选里有测试文件”写成“测试通过”。**
Fake provider 只添加明确的合成说明文件，也不能证明代码缺陷修复有效。

### 3.2 必须实现的下一段

先写/更新 ADR，定义 Runtime Build/Test 的任务、结果和失败契约；先改 Proto，再 generate，
再实现 producer/consumer。使用已有六进程边界：

1. Core 拥有输入、源码、版本和授权事实；Runtime 拥有实际构建/测试及资源隔离；
   Reliability 编排，不直接写 Core/Runtime 的表。
2. Runtime 从可信输入使用原版本固定的镜像和命令，不能执行候选自带的 Containerfile、
   网络配置、挂载配置或自选“总是成功”的测试命令。
3. 固定工具链镜像、关闭构建网络、禁止隐式拉取，限制 CPU/内存/PID/时间/磁盘/输出。
   逐项验证实际引擎能力；不要凭记忆假设 Podman 某 flag 存在或 rootless 必然支持某限制。
4. 构建/测试作业必须有持久幂等、有限重试、取消与重启恢复；不得只保存在 Go map 中。
5. 测试失败、构建失败、超时、进程退出失败、错误回执、输入摘要漂移均不得产生可部署版本。
6. 全部验证成功后，Core 才创建不可变候选版本，并交给 DeploymentController.Offer。
7. **现有 Registry 默认版本按 SemVer 最高版本选取。直接把候选注册成普通高版本会提前暴露。**
   必须设计候选暂存/发布状态，使未通过 canary 的候选不成为默认版本；同时允许受控的精确
   候选 canary 启动。不要把“任务完成”直接翻译成 promote。
8. 安装/version/grant/project revision 的操作前置条件来自持久事实。用户后续升级、撤权、
   卸载或归档不能被重试覆盖。启动失败和 canary 新 incident 都须真实恢复上一精确版本。
9. L5 数据迁移、凭据、权限和资源提升保持人工确认，不包含在自动发布的候选里。
10. 此处 `promote` 仅指 WorkOS 内部已验证 App 候选转为正式版本，不是 Git 分支合并。
    如果构建产物涉及 Git checkout，只能在 Runtime 作业拥有的隔离目录操作，不能修改本仓库
    main、创建本任务辅助分支或把候选代码提交进本仓库。发布决策必须有持久前置条件和审计，
    重复 promote/旧 revision/重复回执不得绕过 canary 或覆盖用户版本。

必须新增真正穿过上述 producer/consumer 的门禁：成功、测试失败、构建失败、启动故障、
canary 故障、回滚失败后重试、进程重启、重复/丢响应、用户版本变更。断言实际版本、
Surface 和持久台账，不只断言返回码。不能用固定成功 adapter 填空。

### 3.3 R2 还缺 Recovery 治理

目前 RepairAdmission 仍通过普通 TaskRouter，绑定项目 Provider；检查健康和候选能力，
但没有完整的 Recovery fallback 和 L3 审批/预算/配额/断路/usage 链。

- 健康且能力满足的 Project Harness 优先；不满足时按明确的 Recovery 配置选择。
- 两级均不可用，产生准确的不可用/等待人工结果和已有通知，不无限重试。
- 重放保留第一次 Provider、目标与策略快照，不随当前健康/配置漂移。
- App-principal 治理依赖 installation grant，不能伪装 App 身份来复用它。
  需要明确 Repair principal 的授权和策略语义，并复用合适的中立 port/事务能力。
- Generic CLI 尚无 hard token/cost budget、usage 和 kernel isolation，不能把能力 bool 改成
  true 或删掉预算检查来“接通 Recovery”。先实现保护，无法支持的组合明确 unavailable。
- 缺少 build 的旧 App 版本不能自动重建；当前 worker 会明确失败，不从 maintainer 字符串
  或运行镜像猜源码。若改善入队体验，必须先在 admission 拒绝并证明零副作用。

### 3.4 监督与遥测也必须收口

不能仅凭旧 `test-real-supervision`/`test-telemetry` PASS 跳过：沿 workload 真实观察 →
incident 去重 → restart/stop → 修复触发追踪全链，验证重启次数、退避、故障窗口、
恢复后新故障及 Runtime/Reliability 重启的持久收敛。区分 fixture engine 与真实 rootless 引擎。
System Monitor 必须消费真实归因的 CPU/内存/进程/故障事实；采集失效展示 unavailable/stale，
恢复后追平，不能把零值当健康。白名单断言日志与遥测无凭据、源码全文或用户内容。

## 4. 接着完成 R3：真实远程 Surface 栈

先审查 `internal/runtime/`、`clients/app-host/`、`sdk/app-sdk/` 与 browser/native 相关
Proto、fixture 和门禁。Bridge 与 Declarative 已有实现，保留其授权和尺寸/嵌套预算。
主要缺口是实际 Remote Browser Pool、Native Runner、WebRTC、Human Native Workspace/PTY。

- Browser Pool 必须有真实 Chromium workload、会话创建/关闭/崩溃恢复、资源限制及真实画面。
  普通 iframe 或静态 browser-worker-fixture 不能证明浏览器池完成。
- Native 必须实现虚拟显示 → 编码 → loopback WebRTC 视频 → Desktop 渲染，以及 Data Channel
  输入回传，Gateway 负责鉴权/信令。黑屏/固定图片/占位页面不能作为 PASS。
- PTY 必须是 owner 明确启动、可监督、可停止的真实进程，字节流/会话/输出有界且归因正确。
- 旧 session、撤权、关闭后旧输入、恶意输入洪泛、worker OOM/崩溃、重启都要测。
- 未声明的剪贴板、文件选择、宿主窗口输入不得协商或转发。
- 不新增第七个常驻 WorkOS 服务；受既有进程管理的子进程/容器符合既有边界。
- 环境缺少真实显示/引擎时，完成不依赖它的软件并明确区别 fixture 证据与真实内核证据。

R3 还必须逐项复核 Bridge 的 agent run/watch、knowledge、notification、project/theme、
files read/write、artifacts create/open、window title/close/maximize/minimize。列出各能力的
manifest/grant、Core/Runtime/shell authority 和实际消费者；缺少对应实现就完成，不增加
一套同义权限。测试越 project/installation/session、符号链接逃逸、超大文件/目录、撤权、
窗口关闭和版本切换后的旧端口。Declarative 复核版本、未知组件、深度/节点/字节预算与安全降级。
Browser、Terminal 必须接入 R6 的真实系统窗口，不以孤立 demo 页面结束。

## 5. 完成 R5：移动原生、通知与局域网全部收口

`apps/mobile-shell` 当前主要是工具库，状态 scaffolded。缺少可启动 HTML/mount 和
Android/iOS 平台工程。旧 wrapper PASS 已撤销；`make test-mobile-wrappers` 对软件缺口应 FAIL。

1. 实现可启动页面/入口，挂载真实共享 shell，接通现有身份/配对/TransportProvider。
2. 建立 Capacitor 平台工程和可复现 sync/build 配置；不要只生成 library main.js。
3. 接通设备密钥的原生安全存储、Core canonical 推送注册/撤销，以及平台姿态、安全区、
   键盘、折叠屏等共享壳适配。不要恢复已删除的无 caller registerPushToken 或另造一套 DTO。
4. 先在容器内完成能验证的 web build、启动、测试、平台源文件和 sync。
5. 只有 Android SDK/Xcode/签名/真实设备等确实缺失时，才记录具体 native binary blocker。
   “没有 Android/iOS 源工程”和“没有 dist/index.html”是软件未实现，不能报环境阻塞。
6. 没有真实 APNs/FCM 凭据就不声明远端投递成功；继续使用已有加密 fixture 软件链。

### 5.1 通知完整路径，不仅是 wrapper

逐项核查并修复：订阅/停用、owner/device 绑定、推送偏好与 UTC 免打扰、通知搜索、
双设备已读收敛、权限拒绝、前台事件与 durable 补收、Core outbox 加密 relay 重试、
Service Worker wake 后回源。验证 VAPID 轮换/旧订阅、过期 token、撤销后不再投递、
重复和乱序、Core/relay 重启、浏览器重新打开；不能靠删 outbox 或清库修复状态。
relay 可见字段保持隐私白名单，不能发送项目名/正文/源码。真实 APNs/FCM 缺凭据只阻塞
真实投递证明，不阻塞 adapter、原生 token 绑定和可复现 fixture 链的实现。

### 5.2 LAN 发现与身份链

核查服务广播 → 客户端发现 → 证书指纹显示/核验 → 配对 → LanDirectTransport →
桌面/移动 RPC 的完整使用路径。覆盖冒名服务、指纹改变/不符、过期配对、设备撤销、
断网/重连和多个发现结果的有界处理。不要以读取固定 mDNS JSON 代替实际广播/发现。
Relay/Overlay 保持原计划的接口与诚实 unavailable，不擅自扩展为公网服务。

## 6. R1、R4、R6 必须逐项修复和验收

### 6.1 R1 Provider、凭据与 Agent 体验

1. 从 HarnessSettings 选择/绑定 Fake、Generic CLI、DeepSeek、Codex、MCP；使用本地
   versioned fixture 跑任务，核对 UI 状态与 Core/Harness 实际 Provider 一致。
2. 健康/能力不满足时拒绝且无入队/预算/key 消费；审批后、执行前重新验证适用的策略事实。
   已入队任务重放不能漂移 Provider、项目 revision 或输出协议。
3. 凭据 purpose/owner/task lease 绑定、撤销、过期、轮换和受审计揭示逐项验证；App 只能拿
   capability。单数据库保持单 owner，越权用合法独立身份/fixture，不能破坏数据库约束。
4. 真实子进程崩溃、静默、乱序、无完成事件、超预算、超时、取消、子孙进程残留和 Core/
   Harness 重启不得挂死任务或伪报成功；usage 与预算只声明实际强制执行的能力。
5. review/context/artifact 发布与 App Agent API 的审批、配额、断路不能被新增 repair 路径绕过。
   不要求所有 Provider 假装具有相同能力；原计划承诺的适配器软件链必须完整，未支持的组合明确拒绝。

### 6.2 R4 工作区、语义知识与归档

1. 用户能注册源、浏览/打开文件、查看状态、重扫、停用、重新启用、重建和归档；追踪
   Desktop → Core → indexfeed → Indexer → 查询/打开的整条路径，不只检查 repository。
2. 目录消失、权限变化、符号链接、路径逃逸、敏感/二进制/超大文件与扫描数量预算有明确结果。
   扫描失败不能把未扫描内容静默判为已删除，也不能继续泄露已停用源。
3. 新增/修改/删除、实时摄取与重建并发、Indexer/Core 重启、重复事件/游标恢复、停用/
   project archive 后迟到 upsert 均验证；tombstone 与 generation 不得被旧事件复活。
4. 保留固定真实离线模型/384 维向量，验证模型不可用、摘要漂移、批量冷启动/缓存命中和
   输入预算；失败明确 degraded/unavailable，不换回随机或词项哈希向量。
5. 中文/英文及不共享关键词的语义查询，混合结果同分、多页、并发更新、游标签名/过期与
   source 过滤必须确定；逐项核对 review artifact/workspace/archive 的打开与版本事实。
6. App scoped 搜索/读取在 grant 撤销、版本变化、跨项目访问时零泄露；archive 的分页、
   对象大小、摘要和读取授权保持有界。发现未接通 UI 的能力要接通并提供证据。

### 6.3 R6 桌面界面质量与所有系统入口

用户原始反馈是“界面设计差、功能没实现好”。已有 21 组截图不能代替本轮实际使用与优化。
先逐张查看 current，再使用真实 fixture 打开每个入口，记录具体交互/布局问题并修复。

- Home/Mission Control：项目创建/切换、任务/未读/告警摘要与真实状态一致，空态可操作。
- Command Palette：键盘上下/确认/Escape、焦点回到原窗口、有界搜索、失效目标重验；
  ask/run/open/switch 等已承诺动作必须接真实服务，不执行任意 shell 命令或 URL。
- Files/Docs/Code/Artifacts/Knowledge：浏览、选取、打开、代码/diff/Markdown 阅读与原权限链
  一致；加载/空态/错误/大内容均可用，项目快速切换不会显示上一项目迟到响应。
- Browser/Terminal：消费 R3 的真实会话和输入链；先修复已有入口再连接新能力。外部网页
  的 top navigation、弹窗、`_blank`、关闭/重开与会话过期必须符合既有窗口安全边界。
- Agent Center/Approvals/Usage、App Library/安装/升级/回滚、HarnessSettings、System Monitor、
  Device Center/配对、Notification Center 都要实际走主路径与失败重试，不只优化 Home。
- 统一间距、字体层级、图标、按钮/输入/菜单、滚动、loading/error/empty、禁用与反馈文案。
  检查文本截断、表格溢出、重叠、对比度、点击面积、焦点可见和键盘可达；去掉无作用按钮。
  不恢复永久侧栏，不为“重设计”重写已验证的窗口与权限模型。
- 拖动/缩放/吸附/最大化/最小化/恢复/关闭、层级与焦点、dock、窗口状态持久化、设备类别
  隔离全部复核。compact 的 bottom nav/sheet 与 composer、安全区、软键盘不能互相遮挡。
  medium/expanded/fold-separated 使用各自布局，双屏/折叠铰链不得截断主交互。
- 在 1440×900、820×1180、390×844 及确定性 fold fixture 验证；每个实际改动的界面至少有
  正常/空态/错误等相关状态证据。使用新的任务级目录保留 before/after，并更新各 client
  current 和 notes；不可覆盖旧历史 before/after 来掩盖变化。

已验证且无缺陷的功能保留；以上检查发现的 bug 和缺口属于本轮实现范围，必须修复。
最终任务记录列出实际修复内容与证据，不能仅写“R1/R4/R6 已检查”。

## 7. 环境和运行纪律

- 宿主不能安装 Podman/SDK、使用 sudo 或修改 systemd；容器内工具/依赖和公开模型可用。
  不调用收费 Provider，不读取真实 APNs/FCM 或其他用户凭据来做测试。
- PostgreSQL 原 volume 必须保留。001–049 已应用，不得修改；下一 migration 为 050。
  本系统每数据库只允许一个 owner；测试不要靠删除 users_single_owner_idx 创建第二 owner。
- 独立 gate 使用自己命名的 scratch DB/容器/临时凭据，只清理这些资源。禁止清全库、
  docker compose down -v、volume prune、清空模型缓存或覆盖用户 workspace。
- Docker 若恢复后 PG 停止，先检查并启动原 postgres 容器，不要“初始化干净环境”删除数据。
- 现有 rootless blocker：宿主没有 podman，cgroup v2 与 user namespaces 可用。
  重新探测并记录当时结果；缺一个工具不等于剩余软件可跳过。
- `tmp/embedding-model` 是已验证的离线模型缓存，保留。固定模型为 multilingual-e5-small，
  revision `614241f622f53c4eeff9890bdc4f31cfecc418b3`，384 维；配置在
  `internal/indexer/adapters/localembedding/model.json`。不要换为随机/哈希向量。
- Go 使用固定 Docker 工具链，Node 使用 Node 24 容器；宿主 Node 22 不适合 pnpm 11。
- 本机构建可能需要 `WORKOS_BUILD_NETWORK=host` 使用既有网络代理；不要打印代理环境值。
- `workos:dev` 是共享镜像 tag。构建新镜像不等于默认六进程全部更新；检查运行容器版本。
  此次合并不自动全栈部署。默认 Core 在 91ce9af 对应功能，Harness 在 CLI v2 基线，
  Gateway 在源码 API 基线；新 producer 如需默认栈验收，Core/Harness 必须成组更新。
  其余 Runtime/Reliability/Indexer 也必须按依赖评估，保留各自配置、凭据和模型挂载。
- 不盲跑会重配默认栈的 Make target；先阅读 target。优先复用独立 gate。
- 只改不可见实现时无需重复截图；改变用户可见界面必须固定 fixture、viewport，保存任务
  before/after/current，并实际打开图片检查布局与可用性，不只生成 PNG。

## 8. 可直接使用的验证命令

从仓库根目录运行；日志放 tmp，失败必须检查内容与退出码。不要把管道最后一个成功命令
的退出码当作测试成功。生成不能和 Go/TS 编译或镜像 COPY 同时进行。

```sh
make generate

make check GO_RUN='docker run --rm --user 1000:1000 -e HOME=/tmp -e GOPATH=/tmp/workos-go -e GOMODCACHE=/go/pkg/mod -e GOCACHE=/workspace/tmp/go-build-cache -e GOPROXY=https://goproxy.cn,direct -v /home/aquatao/workos:/workspace -w /workspace -v workos-go-cache:/go/pkg/mod golang:1.26.7-bookworm'

docker run --rm --user 1000:1000 -e HOME=/tmp -v /home/aquatao/workos:/workspace -w /workspace bufbuild/buf:1.55.1 breaking --against '.git#branch=main'

WORKOS_BUILD_NETWORK=host make test-generic-cli
WORKOS_BUILD_NETWORK=host make test-app-build-inputs
WORKOS_BUILD_NETWORK=host make test-repair-sources
WORKOS_BUILD_NETWORK=host make test-repair-producer
```

最近四个 gate 的区别：generic-cli 验证 review/context/健康切换；app-build-inputs 验证上传和
manifest 绑定；repair-sources 由测试程序担任 mTLS worker 验证 lease/重启/取消；
repair-producer 才包含真实 Harness worker 与 CLI 子进程产出候选。全部均未实现 Runtime build。

真实 PG 与 race 的例子：

```sh
docker run --rm --network host --user 1000:1000 -e HOME=/tmp -e GOPATH=/tmp/workos-go -e GOMODCACHE=/go/pkg/mod -e GOCACHE=/workspace/tmp/go-build-cache -v workos-go-cache:/go/pkg/mod -v /home/aquatao/workos:/workspace -w /workspace golang:1.26.7-bookworm go test -tags='integration repairsource' -count=1 -run '^TestRepairSources|^TestAppSource' -v ./tests/integration

docker run --rm --user 1000:1000 -e HOME=/tmp -e GOPATH=/tmp/workos-go -e GOMODCACHE=/go/pkg/mod -e GOCACHE=/workspace/tmp/go-build-cache -v workos-go-cache:/go/pkg/mod -v /home/aquatao/workos:/workspace -w /workspace golang:1.26.7-bookworm go test -race ./internal/harness/... ./internal/core/agent/... ./internal/core/orchestration/... ./internal/core/appregistry/...
```

最终还需按改动范围审查并运行原计划的综合集成/E2E/专项门禁。`make check` 包含 Go tests、
架构/lint/格式、TS checks/tests 和桌面构建，但不等于所有 tagged integration、真实进程、
真实浏览器、native binary 和视觉检查都运行了。

### 8.1 最终验收清单不能省略

先读 Make target，确认是否会重配默认栈；按依赖准备同一最终版本的验证环境，再串行运行。
不得盲跑 `test-integration` 覆盖不同版本的默认容器配置；需要隔离环境就完善可复现门禁，
不能以“target 会改默认栈”为由省略跨进程验收。以下是检查清单，不是忽略退出码的批处理：

```text
全局：make generate（再次运行无生成漂移，含 README/sqlc）
      make check / make test-integration / make test-e2e / docker compose config --quiet
R1：make test-credential-vault / make test-credential-vault-expansion
    make test-deepseek-fixture / make test-deepseek-structured-review
    make test-codex-harness / make test-mcp-harness / make test-generic-cli
    make test-task-submission-identity / make test-artifact-review / make test-artifact-context
R2：make test-rootless-runtime / make test-podman-fixture / make test-real-supervision
    make test-telemetry / make test-repair-target / make test-app-build-inputs
    make test-repair-sources / make test-repair-producer / make test-repair-deployment
    make test-app-version-rollback
R3：make test-app-bridge-full / make test-declarative-surface / make test-browser-surface
    make test-remote-native-surface / make test-app-files / make test-app-artifacts
R4：make test-semantic-knowledge / make test-local-embedding / make test-model-postgres
    make test-workspace-indexing / make test-workspace-browser / make test-workspace-model-capacity
    make test-project-knowledge-search / make test-app-knowledge-search
    make test-project-knowledge-rebuild
R5：make test-push-relay / make test-push-wake / make test-mobile-wrappers
    make test-mdns-discovery / make test-lan-pairing / make test-notification-center
    make test-incident-notifications / make test-app-notifications
R6：make test-desktop-system-apps / make test-adaptive-shell + 更新的视觉证据
```

对每项记录最终代码 SHA、命令、退出码、实际覆盖、日志路径、PASS/FAIL/BLOCKED。
修复旧 gate 的假 PASS 和漏断言；新 Build/Test/发布、Browser/Native/PTY、mobile 组合链需要
新增或扩展门禁。保留旧门禁名字不代表已经覆盖新增功能，不能只复跑旧断言就结束。
失败先分类并修复；真环境阻塞必须有探测输出和剩余软件验证结果，不能 skip 后计 PASS。
跨进程重启、幂等竞争、存储损坏、依赖失联、private RPC 隔离、身份头清除、gzip/pre-decode
预算、migration forward/checksum 与相关 Go race 均纳入最终证据。最终 staged diff 审查
凭据、私钥、用户内容、大二进制、临时报告等不应入库的内容。

## 9. 必须反复检查：至少三轮不同角度

不要“一次 test 绿了就结束”。每轮记录发现、修复、命令和证据：

1. **实现自查**：沿真实调用链从 UI/RPC 到持久层再到真实副作用逐步追踪。核对每个按钮/能力
   是否有实现、是否能被当前身份使用、失败是否真实、有没有丢弃错误或固定成功。
   对新文件与修改文件逐项审查，不只读 diff 的标题。
2. **失败与重启检查**：先复现，再修复，再重复受影响门禁。覆盖边界、权限、并发、取消、
   超时、丢响应、重复请求、进程重启和上游不可用。检查写库/产物/事件数量，不能只测 HTTP 200。
   禁止放宽断言、skip 故障场景或关闭安全约束来让测试绿。
3. **完成前独立复核**：以最终代码重新检查原验收清单，运行完整必需检查；再次 generate 并
   确认没有生成漂移；检查 git diff --check、状态与文档；逐张查看新增视觉证据。
   找到问题就修复，并重新执行受影响检查，直到证据一致。

“反复检查”不是机械重复同一成功测试，而是不同角度核对真实行为；改动后必须再验证。
必须明确区分：已实现且端到端验证、只验证 fixture 的软件链、尚未实现、真正环境阻塞。
未实现保护不得报 available，无端到端证据状态最高 scaffolded。

最后同步代码、测试、模块文档、总任务记录与 docs/status.json；README 状态块只由工具生成。
有未决项就逐条交代原因和下一步，不伪造 done。用户未授权推送，禁止 git push。

## 10. 停止与最终交付标准

只有当六条工作流验收行全部得到裁决，且所有可在授权环境内完成的软件缺口都已修复、
界面有视觉证据、完整必需检查通过，才结束本批实现。真正环境阻塞的子能力单列保留
scaffolded/unavailable；只要还有待实现的软件项，总任务就不能 done。

工作量大、上下文用尽、镜像构建慢、已经做了若干提交都不是缩减任务或要求用户说“继续”
的理由。跨会话把下一项和依赖写回同一任务记录；恢复后继续，不能重新创建分支/任务记录。
需要用户决定的真实外部前提按原计划记录，先完成所有不依赖该决定的工作。

最终交付必须包含六条工作流逐项结论、修复清单、阶段提交 SHA、Proto/ADR/migration owner、
全部门禁结果、视觉路径、环境阻塞及下一步、`docs/status.json` 的诚实状态，以及最终
`git status --short --branch`。保持唯一任务分支的干净 HEAD；**未经后续明确授权不合并 main，
不 push。** 不以“准备 promote”或“没有 branch protection”绕过这个条件。
