# P3 reliability closeout — 2026-09-19

Branch: `feat/v2-p3-acceptance-complete`, based on `e6698b3`. The isolated gate owns its PostgreSQL, six process containers, Docker namespace and scratch artifact root. No real provider credentials or paid model were used. All gate projects were removed after the runs, including the deliberate SIGTERM case; shared development services were not touched.

## Added and verified

- `TestP3Closeout/F06_cancel_wins_at_*`: cancellation during artifact-byte persistence, preparing metadata and immediately before verdict all wins; no ready artifact or deployment side effect.
- `TestP3Closeout/F07_idempotent_submit_and_drift`: four concurrent identical requests produce one job; owner, project, installation, incident, app, source identity/digests, manifest digest, build/test commands, output directory, runtime command, candidate bytes and image drift abort without replacing it. Contradictory input facts are invalid.
- `F09_operator_import_drop_reply_replays_one_identity`: admin import commits then loses its response; same-key retry returns the exact persisted ID with one row.
- `F10_same_bytes_keep_distinct_source_metadata`: same bytes with different import keys/apps retain independent artifact provenance while sharing owner/digest bytes.
- `F13_verdict_promotion_gates_on_bytes`: durable successful verdict followed by real Runtime kill/restart converges to one ready artifact preserving exact artifact ID, digest and job ID. A concurrent reconciler may promote before the process is killed, so either `preparing` or `ready` is valid at the barrier.
- `F19_grant_epoch_revokes_old_bridge_and_regrant_uses_fresh_surface`: actual project.read grant epoch 1→2→3; old session token remains denied after revoke and regrant; a fresh surface gets only the currently granted capability. The test supplies its real session device identity to Runtime's Bridge authorizer.
- `F10_same_bytes_keep_distinct_source_metadata`: after the distinct-source assertion, corrupt the shared digest file and verify a new import rejects the invalid dedupe hit without creating metadata; original bytes are restored.
- `F23_owner_quota_counts_orphaned_disk_bytes`: a real Runtime admin import against an owner directory containing a sparse 2 GiB orphan is rejected, with no artifact row.
- `F24_registry_rejects_foreign_owner_and_wrong_app_artifacts`: Core refuses both another owner's ready artifact and a same-owner artifact imported for a different app; neither refusal creates an `app_versions` row.
- `F25_late_surface_receipt_cannot_touch_new_generation`: after publishing B and rolling back to A, Docker contains exactly one app container; closing the stale B Surface leaves the same A generation and container count intact.
- Modified-source `sh tools/v2-p3-delivery/gate.sh`: PASS, evidence run `tmp/v2-p3-delivery.4SvAHq/`. This included the complete existing P3 delivery chain, all `TestP3Closeout`, F11–F17 persistence windows, the full F18–F25 matrix (including new F19/F23/F24/F25 assertions), both real Chromium business E2E specs, and Core/Runtime/Reliability restart replay.
- Modified-source `sh tools/v2-completion/gate.sh`: PASS, evidence run `tmp/v2-completion.azZwbx/`. The native/session, PTY, workspace isolation, preview, App bridge/grants/installation integration and shared-files/resumed-native/preview business E2E passed. The gate reported the three fixed-viewport visual specs as skipped; they are not claimed as passed visual checks. The gate was run directly from the host because its fixture probes host-loopback ports.
- Legacy profile: `sh tools/v2-p3-delivery/gate.sh legacy` PASS, covering repair success/failure, user version change, Runtime restart restore, recovery fallback/awaiting_manual, and bounded deployment rollback matrix.
- SIGTERM cleanup: sent SIGTERM to the bundle gate while a real delivery E2E was active; after the in-flight child ended, the gate exited with result 143 and removed all containers in its unique `p3-v2-p3-delivery-jxhxgi` project. The interrupted bundle run itself is not a passing acceptance run.
- `make generate`: PASS; generated Go/TypeScript had no diff. Status rendering updated the generated README from the status source. `make check`: PASS after formatting the updated task Markdown; buf format/lint, sqlc vet, Go gofmt/vet/test, architecture, ESLint/Prettier, workspace checks, desktop-web production build and generated-status check all completed. The first check correctly failed on the new task document's Prettier layout; after formatting, the complete check passed.

## Limits and follow-up

During implementation, the first modified-code bundle attempts caught and fixed an F10 query that included F09's same digest and an F13 assertion that incorrectly required the artifact to remain preparing despite concurrent reconciliation. The final modified-source bundle gate then passed in full. The V2 completion gate initially ran from the `workos-make` tool container and could not reach its host-loopback Gateway fixture; rerunning the same gate directly from the host passed. The separate SIGTERM cleanup exercise intentionally interrupted one earlier bundle run; its gate exited with result 143 and cleaned its unique Compose namespace, while the final full bundle gate subsequently passed.

Remaining partial scenarios are F07 drift-after-restart combinations; F09 response loss for all result/query paths; F10 cross-owner dedupe/source combinations; F14 interruption before candidate registration; F16 full Runtime mid-chain identity-drift combinations; F23 quota accounting for each ready/preparing/staging category; F24 cross-project and build/import impersonation combinations; and F25 stale stop/generation and unrelated-container cleanup. Fixed-viewport visual specs were skipped by the V2 completion gate. P3 remains `scaffolded` until these evidence gaps are closed; a green full gate does not promote a partially covered row to PASS.
