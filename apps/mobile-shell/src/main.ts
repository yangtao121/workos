// The mobile entry mounts the same shared adaptive shell the desktop uses;
// device-specific behavior (posture, safe area, keyboard) stays inside the
// shell contract rather than forking per platform.
import "./index.js";
