# Admin CLI

The first admin CLI is implemented by the Go `workosctl` binary. This directory is reserved for
future TypeScript-based interactive administration surfaces; do not duplicate `workosctl` commands.
