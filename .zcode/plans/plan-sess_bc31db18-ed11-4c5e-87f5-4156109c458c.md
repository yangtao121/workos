## 目标

在 `/home/aquatao/workos/docs/prompts/` 生成**一份** house-style 总 Prompt，覆盖上一轮分析出的全部未开发缺口，交给后续智能体按批次执行。本次只生成文档，不写代码、不建分支、不改 status.json。

## 产出文件

```text
docs/prompts/20260903-next-agent-remaining-capability-sweep.md
```

单一文件，中文，对齐现有 `next-agent` 惯例（如 20260902-local-first-notifications），预估 1200–1600 行。因规模远超单批 10–14 小时，Prompt 内置"跨会话续作协议"：单 branch + 单任务记录作为 durable state，每阶段聚焦提交，会话中断后从任务记录恢复。

## 写入前的事实核对（只读）

引用仓库事实前先核对，避免 prompt 里写死过期编号：
- 当前 `main` HEAD（快照为 6853b83，写入时复核）
- 各进程 migration 最新编号（Core 至 031、Reliability 030、Indexer 026–028、Runtime 015/018）
- ADR 最新编号（0014，下一个 0015）
- Makefile 既有门禁目标清单
- App Registry capability vocabulary 现状

## Prompt 文档结构（16 节，对齐 house style）

1. 标题与交接说明（长时间自主执行、可能跨多会话、单一 branch/worktree/任务记录/写入智能体）
2. 角色与唯一最终目标（六大 workstream 全部达到有真实证据的 working/scaffolded 升级）
3. 单分支纪律 + **跨会话续作协议**（任务记录即恢复点；环境阻塞时记录 blocker 并切换到不依赖该环境的下一批次，不整体停工）
4. 无人值守与工作量安排（W1–W6 串行顺序表 + 环境依赖标注）
5. 编写时仓库事实（注明"执行时必须重新核对"）
6. 开始前必读清单（AGENTS/structure/implementation/status/ADR/tasks/关键代码区）
7. 全批次不可违反边界（架构与数据所有权、身份安全内容隐私、真实能力与诚实状态；含 Podman/外部账号/宿主软件的统一处理规则）
8–13. **六大 Workstream 章节**（见下）
14. 专项门禁总表（新增 make 目标）与全局失败矩阵
15. 明确不在范围内 + 停止并请求用户的条件
16. 最终验证、文档/状态/完成定义、最终交接格式

## 六大 Workstream 内容要点

**W1 Provider 与凭据扩展**：Codex Harness Adapter（App Server JSON-RPC → canonical 事件、capability 诚实声明、task credential lease 复用 ADR-0009 模式）；MCP Agent Adapter；Credential Vault 扩展（Codex/GitHub/云凭据类型、secret 查看/导出 UI、master-key 在线轮换、多 owner）。门禁：test-codex-harness、test-mcp-harness、test-credential-vault-expansion（全部用本地 fixture，不访问真实厂商）。

**W2 真实 Runtime 与自愈链**：rootless Podman 真实验收收口 ADR-0006（容器 Web Service Surface + cgroup v2/PSI 跨进程浏览器证据，宿主不满足则走 blocker 协议）；真实 observation→Incident→restart/stop 监督链；otel-collector 遥测与 System Monitor 接入；Repair Orchestrator（Incident → Task Envelope incidentId → Project/Recovery Harness，L3）；Deployment Controller（Build/Test/Canary/Promote/Rollback，L4，衔接 ADR-0012）。门禁：test-rootless-runtime、test-real-supervision、test-repair-deployment、test-telemetry。

**W3 Surface 与 Bridge 补全**：App Bridge 剩余能力（window.*、project.current、files.*、artifacts.open/create、theme.get，capability vocabulary + consent + epoch 重验）；Declarative Surface（结构化 UI 原生渲染，manifest 新 renderer 类型）；远程 Surface 栈（Remote Browser Pool、Native Runner、WebRTC Remote Native Surface、Human Native Workspace，Gateway WebRTC signaling）。门禁：test-app-bridge-full、test-declarative-surface、test-remote-native-surface。

**W4 语义知识**：pgvector embedding 管道（Indexer 拥有）、workspace/文件源摄取（project mounts）、通用 archive/object store、词法+语义混合检索。门禁：test-semantic-knowledge、test-workspace-indexing。

**W5 后台推送与移动原生**：Push Relay 模式（只发加密 notificationId，APNs/FCM 真实账号为停止条件，fixture relay 先行）+ Web Push/Service Worker + 偏好/免打扰 + 有界通知搜索；Capacitor iPad/Android 封装（原生安全存储、push、折叠屏 posture，Xcode/Android SDK 为停止条件）；mDNS 发现 + TransportProvider 接口与 LanDirect 验收。门禁：test-push-relay、test-mdns-discovery、test-mobile-wrappers（构建级）。

**W6 桌面系统应用与入口**：Command Palette ⌘K、Project Mission Control（expanded）、Dock 系统应用（Home、Files、Docs、Code、Browser；Terminal 明确依赖 W3 native runner；Experiments 定位为 project app 模板/占位）、窗口 snap 增强。门禁：test-desktop-system-apps + 全量多 viewport 视觉证据。

执行顺序：W1 → W2 → W3 → W4 → W6 → W5（契约先行、环境强依赖后置；任一 stream 遇环境阻塞即记录并切换）。

## 明确排除（写入"不在范围内"节）

多人协作、复杂知识图谱、完全自动语义修复（L5 人工确认保留）、多 Harness 同时协作一个 Project、复杂自由窗口动画、公网商业信任根；各 stream 另有各自的非目标清单。

## 停止条件（写入 Prompt）

真实 APNs/FCM 账号与凭证、Xcode/Android SDK 安装、宿主 Podman 安装/提权、公网暴露信任决策、v1 字段号破坏性变更、已执行 migration 修改、第七个常驻进程等——均停下请求用户；普通实现选择不构成停止理由。

## 验证

- `git diff --check`；文件为唯一新增，工作树其余干净
- 通读核对：六个进程边界、模块依赖方向、migration/ADR 编号、make 目标名与现有 Makefile 不冲突、引用的 docs 路径真实存在
- 不运行 make generate/check（无代码变更）